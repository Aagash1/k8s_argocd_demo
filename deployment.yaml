apiVersion: apps/v1
kind: Deployment
metadata:
  name: k8s-app-demo
  labels:
    app: k8s-app-demo
spec:
  replicas: 2
  selector:
    matchLabels:
      app: k8s-app-demo
  template:
    metadata:
      labels:
        app: k8s-app-demo
    spec:
      containers:
        - name: k8s-app-demo
          image: Aagash1/k8s_app_demo:358d8518
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "250m"
              memory: "256Mi"

 
