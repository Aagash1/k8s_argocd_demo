apiVersion: v1
kind: Service
metadata:
  name: k8s_app_demo
  labels:
    app: k8s_app_demo
spec:
  type: LoadBalancer
  selector:
    app: k8s_app_demo
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80

 