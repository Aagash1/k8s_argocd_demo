apiVersion: v1
kind: Service
metadata:
  name: k8s-app-demo
  labels:
    app: k8s-app-demo
spec:
  type: LoadBalancer
  selector:
    app: k8s-app-demo
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80

 
